const getX =(object) => {
  return object.x
}

const point = (x, y) => {
  return {x: x, y: y}
}

const emptyObject = () => {
  return {}
}

const distance = (x1, x2, y1, y2) => {
  return Math.sqrt(Math.abs(x1.x1 - y1.y1)+Math.sqrt(Math.abs(x2.x2 - y2.y2)))
}

const midpoint = () => {

}