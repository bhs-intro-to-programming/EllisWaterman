const getX =(object) => {
  return object.x
}

const point = (x, y) => {
  return {x: x, y: y}
}

const emptyObject = () => {
  return {}
}

const distance = (x, y) => {
  return Math.sqrt(Math.abs(x1-x2)**2+Math.abs(y1-y2)**2)	
}

const midpoint = () => {
  return
}